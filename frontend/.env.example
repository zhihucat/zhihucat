# Local development
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
# Supabase project (the publishable/anon key is safe to expose in a browser when RLS is enabled).
NEXT_PUBLIC_SUPABASE_URL=https://tshojzkaojcehjunhbju.supabase.co
# Copy the Publishable key from Dashboard → Project Settings → API.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Older projects may still label this key as anon; either variable is supported.
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Production/Zeabur/Vercel
# NEXT_PUBLIC_API_BASE_URL=https://argus-api.tomeet.chat
