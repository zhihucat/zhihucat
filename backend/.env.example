PORT=4000
HOST=0.0.0.0
# Set this to the deployed frontend origin(s), comma-separated for Vercel previews.
CORS_ORIGIN=http://localhost:3000
# Server-only Supabase credentials. Never expose the service role key to the browser.
SUPABASE_URL=https://tshojzkaojcehjunhbju.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Optional for a single development process; required across backend replicas.
# Generate a random 32+ character secret, keep it backend-only and stable across deploys.
CAMPAIGN_SIGNING_KEY=
# Production example:
# CORS_ORIGIN=https://argus.example.com,https://argus-git-main.example.com
