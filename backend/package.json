{
  "name": "@argus/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone Node.js API for ARGUS+",
  "type": "commonjs",
  "engines": { "node": ">=22.18.0" },
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test"
  }
}
